function rotateImage(a) {
    let newA = a
    // first try to rotate values into a new array to make it 
    for(let i = 0; i < a.length; i++) {
        for(let j = 0; j < a.length; j++) {
            
        }
    }
}